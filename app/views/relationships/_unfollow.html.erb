<%= form_tag url_for(:controller => 'relationships', :action => 'destroy'), method: :delete do %>
    <%= hidden_field_tag :followable_type, @user.class.name %>
    <%= hidden_field_tag :followable_id, @user.id %>
    <%= submit_tag  'Unfollow'%>
<% end %>